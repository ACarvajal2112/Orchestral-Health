// // add item to cart and return array of cart items
// const addItemToCart = (cartItems, itemToAdd) => {
//   const cartItemExists = cartItems.find(item => item.id === itemToAdd.id);
//   if (cartItemExists) {
//     cartItems
//   }
// };

